spring:
  application:
    name: user-portfolio-backend

#  datasource:
#    url: jdbc:postgresql://localhost:5432/portfolio_db
#    username: root
#    password: root
#
#  jpa:
#    hibernate:
#      ddl-auto: update
#    show-sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - email
              - profile

jwt:
  secret: ${JWT_SECRET:dev-secret-key-should-be-at-least-32-chars-long}
  expiration: 900000

app:
  frontend:
    url: ${FRONTEND_URL:http://localhost:3000/}
